;; lazy listsh have not only lazy CDR but also a lazy CAR, so we could have
;; advantage of it as example when making some long (or infinite) trees, so
;; branches will be evaluated only when needed.


